rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // By default, require authentication for read/write.
      // This is a secure fallback boilerplate that can be customized.
      allow read, write: if request.auth != null;
    }
  }
}
