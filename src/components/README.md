# Components Directory (`src/components/`)

This directory houses global, reusable React UI components that are stateless and presentation-oriented.

## Guidelines
- Keep components focused, reusable, and modular.
- Use props to pass styles and configurations.
- Avoid integrating Firebase calls or service workers directly into these components.
